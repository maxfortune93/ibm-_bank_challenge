{
  "version": 2,
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/angular",
      "config": {
        "distDir": "dist/front-ibm-bank"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
